#include "main.h"

/**
 * _isalpha - checks for alphabetic character
 * @: The character to be checked
 * Return: 1 for alphabetic character or anything else
 */

int _isalpha(int c)

{

	if (( c >= 65 && 90) || (c >= 97 && c <= 122)
	{
	return (1);
	}
	return (0);
}
